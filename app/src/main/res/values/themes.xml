<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base View theme (Compose uses its own Theme in Kotlin). -->
    <style name="Theme.MikLink" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
